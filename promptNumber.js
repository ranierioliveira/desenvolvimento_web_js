function promptNumber(enunciado){
    const numeroDigitado = Number(prompt(enunciado));
    return numeroDigitado;
  }
  
  const numA = promptNumber("Digite o número A: ");
  const numB = promptNumber("Digite o número B: ");
  const numC = promptNumber("Digite o número C: ");